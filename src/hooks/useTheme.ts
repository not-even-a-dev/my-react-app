/**
 * Hook to apply theme class to the root element
 * Note: Theme DOM manipulation is handled by uiStore
 */
export function useTheme() {
  // Theme management is handled in uiStore
  // This hook is kept for potential future use
}

